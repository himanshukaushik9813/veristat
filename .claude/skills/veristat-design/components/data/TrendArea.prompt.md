One-sentence: Filled area sparkline with a gradient and a pulsing end dot — the landing stat-panel chart.

```jsx
<TrendArea values={series} id="probes" color="var(--accent)" />
```

Auto-scales to its own max. Give each instance a unique `id` (used for gradient defs).
