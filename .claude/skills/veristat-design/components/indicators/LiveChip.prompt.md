One-sentence: "LIVE" chip — a pulsing green dot + label for live data panels.

```jsx
<LiveChip />
<LiveChip label="PROBING" />
```

Green pulse (respects nothing special; pure CSS keyframes). Used in the probe-activity panel header.
