One-sentence: Amber conflict-of-interest badge for Veristat's own self-listing — the brand names its bias.

```jsx
<CoiBadge />
<CoiBadge>Veristat itself — scored by the same methodology</CoiBadge>
```

Amber outline on a raised surface. Override the text via `children`.
