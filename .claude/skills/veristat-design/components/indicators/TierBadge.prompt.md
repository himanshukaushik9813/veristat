One-sentence: Verification-tier pill (T1 deterministic, T2 consensus, T3 operational-only).

```jsx
<TierBadge tier={1} />
<TierBadge tier={3} />
```

Outlined mono pill; hover title explains the tier. T3 means accuracy is explicitly *not verified*.
