{
  "name": "@veristat/sdk",
  "version": "0.1.0",
  "description": "Pre-purchase verification for the agent economy: check a paid service's verified track record before spending money on it.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@veristat/chain": "workspace:*",
    "@veristat/shared": "workspace:*",
    "viem": "^2.54.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
