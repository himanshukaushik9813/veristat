{
  "name": "@veristat/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "tsx src/main.ts",
    "crawl": "tsx src/main.ts crawl",
    "probe-once": "tsx src/main.ts probe-once",
    "dispute": "tsx src/cli/dispute.ts"
  },
  "dependencies": {
    "@veristat/chain": "workspace:*",
    "@veristat/db": "workspace:*",
    "@veristat/shared": "workspace:*",
    "viem": "^2.21.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
